# com o comando abaixo podemos buscar pela versão mais recente por tentativa e erro. 

pip install python-dateutil==1.4

# com o comando abaixo, conseguimos listar as versões existentes de um pacote

pip install python-dateutil==randomwords

# o comando abaixo verifica versões já instalads por nóes

pip freeze python-dateutil

# atualizar um pacote

pip install <name of package> --upgrade
